import Contacts from '@pages/Contacts';
import Layout from '@pages/Layout';

const ContactsPage = () => {
  return (
    <Layout>
      <Contacts />
    </Layout>
  );
};

export default ContactsPage;
